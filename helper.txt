import React from 'react';
import CategoryItem from './CategoryItem';

const CategorySection = ({ categories }) => {
  return (
    <section
      id="category-wrapper"
      className="py-12 px-6 bg-[var(--color-white-two)]"
    >
      <h2 className="text-3xl font-bold mb-8 text-center text-[var(--color-black-one)]">
        Browse by Category
      </h2>
      <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-6 max-w-6xl mx-auto">
        {categories.map((cat) => (
          <CategoryItem key={cat.name} {...cat} />
        ))}
      </div>
    </section>
  );
};

export default CategorySection;